<!DOCTYPE html>
<html dir="{{ direction }}" lang="{{ lang }}" class="{{ theme }}">
	<head>
		{{ head }}
		{{ microdata.head }}
	</head>
	<body>
		<div id="viewport">
			{{ header }}

			{{ content }}

			{{ footer }}
		</div>

		{{ microdata.body }}
	</body>
</html>
